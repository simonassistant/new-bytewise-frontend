[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.watchPatterns]
include = ["**/*.vue", "**/*.js", "**/*.ts", "**/*.json", "**/*.md"]
